services:
  findata-mcp:
    image: ghcr.io/lakshya-aga/data-mcp:latest
    ports:
      - "8000:8000"
    volumes:
      # Option 2: mount host auth (login once on your machine, no OAuth prompts in container)
      - ~/.codex:/root/.codex:ro
      - ~/.claude:/root/.claude:ro
      # Writable overlays for Codex-generated files (initialized from image on first run)
      - findata-app:/app/findata
      - findata-mcp-app:/app/findata_mcp
      - findata-data:/app/data
    environment:
      FRED_API_KEY: ""
      # OPENAI_API_KEY: sk-...
      # ANTHROPIC_API_KEY: sk-ant-...
    restart: unless-stopped

volumes:
  findata-app:
  findata-mcp-app:
  findata-data:
